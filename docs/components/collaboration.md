# CollaborationSection

`src/components/sections/CollaborationSection.astro` is static.

## Purpose

Shows selected public proof and routes serious visitors to the contact section or GitHub.

## Architecture

This section previously did not need client state, so it was converted from a React/Motion island to static Astro. Hover states are CSS-only.
